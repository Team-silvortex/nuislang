# Versioning Notes

This directory is reserved for future versioning policy documents.

Expected scope later:

* language/toolchain surface versioning
* `YIR` format/version compatibility policy
* `nustar` package format/version compatibility policy
* ABI and loader-contract evolution rules

Current state:

* versioning policy is still mostly implicit in the checked-in implementation
  and reference docs
* no standalone versioning policy document is frozen here yet

For current practical behavior, prefer:

* [../reference/yir-tools-reference.md](/Users/Shared/chroot/dev/nuislang/docs/reference/yir-tools-reference.md)
* current package/manifest parsing code
