protocol = "nuis-self-hosting-readiness-v2"
release_line = "beta-0.10.*"
phase = "stage0-to-stage1-migration"
phase_status = "active"
migration_start = "beta-0.10.*"
completion_window_start = "gamma-0.5.*"
completion_window_end = "gamma-0.10.*"
gate_count = 5

[[gates]]
id = "bootstrap-language-subset"
coordinate = "language-core/nuisc/bootstrap-language-subset"
status = "stable"
progress = 100
required_before = "beta-0.10.0"
next_action = "preserve v8 as the stage0/stage1 dependency ceiling and introduce a new protocol version before admitting another language capability or scalar export"
validation_command = "CARGO_INCREMENTAL=0 cargo test -q -p nuisc command_bootstrap -j 1"
expected_artifact = "nuis-bootstrap-language-subset-v8, nuisc bootstrap-check, twenty-one exact scalar exports, and accepted plus rejected Nuis fixtures crossing the normal semantic pipeline"
blocker = "none for subset v8; chunked individual aggregate projection and aggregate backedge lowering remain independent readiness gates"

[[gates]]
id = "compiler-data-model"
coordinate = "standard-library/std/compiler-data-model"
status = "usable"
progress = 99
required_before = "beta-0.10.0"
next_action = "forward one complete v10 CompilerAggregateArena through a nested Nuis helper while preserving registry provenance, stable indices, canonical identity, and atomic failure"
validation_command = "CARGO_INCREMENTAL=0 cargo test -q -p nuis --test compiler_data_model_bootstrap -j 1"
expected_artifact = "nuis-compiler-data-model-v10 with one registered 24-byte CompilerChunkedPayload projected across three aggregate pages, exact v9/v10 identities, atomic failures, and native exit 130"
blocker = "v10 closes individual payloads larger than sixteen bytes, but collection indices remain bounded to sixteen, defining-module provenance and nested full-arena forwarding are incomplete, and general aggregate loop backedges remain open"

[[gates]]
id = "stage-neutral-ir-boundary"
coordinate = "language-core/nuisc/stage-neutral-ir-boundary"
status = "usable"
progress = 99
required_before = "beta-0.10.0"
next_action = "preserve dual AST/NIR production selection while extending structural pagination or a third registered derived encoding without changing the canonical v1 bundle"
validation_command = "CARGO_INCREMENTAL=0 cargo test -q -p nuis-artifact compiler_stage_handoff_v2 -j 1 -- --test-threads=1 && CARGO_INCREMENTAL=0 cargo test -q -p nuis-artifact compiler_candidate_production -j 1 -- --test-threads=1 && CARGO_INCREMENTAL=0 cargo test -q -p nuis --test compiler_structural_projection_candidate -j 1 -- --test-threads=1"
expected_artifact = "AST and NIR derived stages selected in source order with exact source, transform, payload, checkpoint, recovery, and 2/2 semantic identities beside the canonical v1 bundle"
blocker = "the real candidate closes generic two-encoding production selection, but production still binds only two structural pages per projection and general aggregate loop backedges remain open"

[[gates]]
id = "stage0-stage1-driver"
coordinate = "compiler-toolchain/bootstrap/stage0-stage1-driver"
status = "stable"
required_before = "beta-0.10.0"
progress = 100
next_action = "preserve candidate Nsld input v1 while wider source coverage and real native object bytes advance under separate versioned contracts"
validation_command = "CARGO_INCREMENTAL=0 cargo test -q -p nuis-artifact compiler_candidate_nsld -j 1 -- --test-threads=1 && CARGO_INCREMENTAL=0 cargo test -q -p nsld candidate_input -j 1 -- --test-threads=1 && CARGO_INCREMENTAL=0 cargo test -q -p nuis --test compiler_structural_projection_candidate two_uncached_clean_candidates_bind_one_reproducibility_aggregate -j 1 -- --test-threads=1"
expected_artifact = "one candidate-owned nuis-compiler-candidate-nsld-input-v1 and bound materialization capability tying canonical source/YIR identity to Main.main return-i64 7 while stage0/provider/native-object/replacement/selection remain false"
blocker = "none for the bounded canonical source-to-equivalent-Nsld-input gate; general grammar, unbounded stage representations, and candidate-owned native object bytes remain later migration slices"

[[gates]]
id = "differential-reproducibility-gate"
coordinate = "developer-system/bootstrap/differential-reproducibility-gate"
status = "stable"
progress = 100
required_before = "beta-0.10.0"
next_action = "preserve reproducibility v2 and the immutable signed v1 predecessor while remote v2 mirroring advances under a separate attestation protocol"
validation_command = "CARGO_INCREMENTAL=0 cargo test -q -p nuis-artifact compiler_component_reproducibility_v2 -j 1 -- --test-threads=1 && CARGO_INCREMENTAL=0 cargo test -q -p nuis --test compiler_structural_projection_candidate two_uncached_clean_candidates_bind_one_reproducibility_aggregate -j 1 -- --test-threads=1 && CARGO_INCREMENTAL=0 cargo test -q -p nuis --test compiler_remote_attestation_evidence -j 1 -- --test-threads=1"
expected_artifact = "nuis-compiler-component-reproducibility-v2 binds each clean root's exact canonical 2/2 representation sidecar beside the unchanged v1 aggregate and leaves replacement authority false"
blocker = "none for the bounded local differential/reproducibility gate; remote v2 sidecar attestation and cryptographic proof of physical independence remain later migration concerns"
