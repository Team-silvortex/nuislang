module_schema = "nuis-stdlib-module-v1"
name = "ns-nova"
package_id = "nuis.ns-nova"
tier = "framework"
depends_on = ["core", "std"]
summary = "GPU-first native rendering and application framework built on nuis heterogeneous execution."

surfaces = [
  "renderer",
  "scene-and-frame-graph",
  "window-input-lifecycle",
  "material-and-shader-packaging",
  "gpu-ui-and-3d-runtime"
]

library_modules = [
  "lib/nova_contracts.ns"
]

library_import_policy = "manual-only"

source_modules = [
  "core/theme_surface.ns",
  "core/frame_runtime.ns",
  "core/texture_resource_recipe.ns",
  "core/window_controls_runtime_recipe.ns",
  "ui/panel_selection.ns",
  "ui/panel_blueprint.ns",
  "ui/window_controls_recipe.ns",
  "scene/scene_runtime.ns",
  "scene/efficiency_runtime.ns",
  "scene/scene_blueprint.ns",
  "scene/window_controls_scene_recipe.ns"
]
