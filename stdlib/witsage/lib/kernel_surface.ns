/// Kernel-facing WitSage surface for classical ML tensor execution plans.
mod kernel WitSageKernelSurface {
  /// Declares the default kernel profile used by WitSage examples.
  fn profile() {
    const bind_core: i64 = 2;
    const queue_depth: i64 = 8;
    const batch_lanes: i64 = 16;
  }
}
