module_schema = "nuis-stdlib-module-v1"
name = "core"
package_id = "nuis.core"
tier = "foundation"
depends_on = []
summary = "Minimal semantics-first foundation."

surfaces = [
  "contract.core.prelude.primitive-values.v1",
  "contract.core.prelude.ref-ownership-conventions.v1",
  "contract.core.prelude.basic-math.v1",
  "contract.core.prelude.structural-source.v1"
]

library_modules = [
  "lib/prelude_contracts.ns"
]

library_import_policy = "project-auto"

source_modules = [
  "basic_scalars.ns",
  "struct_patterns.ns",
  "math_runtime.ns",
  "ref_runtime.ns",
  "value_blueprint.ns",
  "result_patterns.ns"
]
