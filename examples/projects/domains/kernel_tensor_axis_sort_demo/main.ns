use kernel KernelUnit;

mod cpu Main {
  fn main() {
    let input = kernel_tensor(2, 3, "6,2,4,5,1,3");
    let sorted_rows = kernel_sort_axis(input, "rows");
    let pick: i64 = kernel_element_at(sorted_rows, 0, 1);

    print(pick);
  }
}
