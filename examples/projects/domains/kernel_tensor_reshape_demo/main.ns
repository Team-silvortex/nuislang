use kernel KernelUnit;

mod cpu Main {
  fn main() {
    let input = kernel_tensor(2, 3, "2,4,6,1,3,5");
    let reshaped = kernel_reshape(input, 3, 2);
    let picked: i64 = kernel_element_at(reshaped, 2, 1);

    print(picked);
  }
}
