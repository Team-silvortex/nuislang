name = "net_httpish_service_session_packet_recipe_demo"
version = "0.1.0"
entry = "main.ns"
modules = ["main.ns", "network_unit.ns", "../shared/net_httpish_packet_shapes.ns"]

abis = [
  { name = "cpu-main", kind = "staticlib", target = "host-native" }
]
