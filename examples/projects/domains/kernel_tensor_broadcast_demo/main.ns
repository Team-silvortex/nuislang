use kernel KernelUnit;

mod cpu Main {
  fn main() {
    let input = kernel_tensor(1, 3, "2,4,6");
    let widened = kernel_broadcast(input, 2, 3);
    let picked: i64 = kernel_element_at(widened, 1, 2);

    print(picked);
  }
}
