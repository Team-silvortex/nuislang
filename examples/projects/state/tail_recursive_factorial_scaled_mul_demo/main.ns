mod cpu Main {
  fn main() -> i64 {
    let current: i64 = 4;
    let acc: i64 = 1;
    while current > 1 {
      let current: i64 = current - 1;
      let acc: i64 = acc * ((current + 1) * 2);
    }
    return acc;
  }
}
