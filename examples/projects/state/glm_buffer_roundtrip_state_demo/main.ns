mod cpu Main {
  fn main() -> i64 {
    let buffer: ref Buffer = alloc_buffer(3, 0);
    buffer[1] = 7;
    let len: i64 = buffer.len;
    let value: i64 = buffer[1];
    free(buffer);
    return value + len;
  }
}
