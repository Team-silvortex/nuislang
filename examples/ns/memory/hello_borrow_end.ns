mod cpu Main {
  fn main() {
    let head = move(alloc_node(10, null()));
    let head_ref = borrow(head);
    let before = head_ref.value;
    borrow_end(head_ref);
    head.value = 77;
    let after = head.value;
    print(before);
    print(after);
  }
}
