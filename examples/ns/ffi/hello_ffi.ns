extern "nurs" interface HostMath {
  fn speed_curve(value: i64) -> i64;
}

mod cpu Main {
  fn main() {
    let base: i64 = 7;
    let curved: i64 = HostMath.speed_curve(base);
    print(curved);
  }
}
