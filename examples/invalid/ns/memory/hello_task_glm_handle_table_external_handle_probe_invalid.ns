mod cpu Main {
  struct HandleTableTaskPacket {
    handles: HandleTable<FabricBindings>,
    lane: i64,
    domain_code: i64
  }

  async fn consume(packet: HandleTableTaskPacket) -> i64 {
    return packet.lane + packet.domain_code;
  }

  fn main() -> i64 {
    return 1;
  }
}
