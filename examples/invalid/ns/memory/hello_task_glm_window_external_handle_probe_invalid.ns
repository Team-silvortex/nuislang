mod cpu Main {
  struct WindowHandleTaskPacket {
    frame: Window<i64>,
    lane: i64,
    domain_code: i64
  }

  async fn consume(packet: WindowHandleTaskPacket) -> i64 {
    return packet.lane + packet.domain_code;
  }

  fn main() -> i64 {
    return 1;
  }
}
