// Legacy aggregate test module intentionally left empty.
//
// Focused nsld integration tests now live in `main_*_tests.rs` modules so the
// old catch-all file does not grow back into a thousand-line bucket.
