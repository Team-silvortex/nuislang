use crate::dev_tensor::{DevTensorCell, DevTensorExpectedCoordinate};

pub(crate) const DEV_TENSOR_CELLS: &[DevTensorCell] = &[
    DevTensorCell {
        architecture: "compiler-frontdoor",
        module: "nuis-cli",
        function: "workflow-and-project-orientation",
        status: "stable",
        progress: 100,
        bootstrap_critical: true,
        closure_role: "developer-frontdoor",
        evidence: "workflow/project-status/project-doctor JSON and text frontdoors are regression-backed, share closure_summary source/status/ready/blocker/action/command fields, expose the versioned nuis-frontdoor-reading-order-v1 contract in nested and top-level frontdoor surfaces, order closure_summary before the development-tensor handoff contract, document closure_summary as the canonical human closure line, project-status promotes built-project link-plan evidence into project-status-link-plan closure guidance instead of stopping at artifact-ready, nuis status prints the stable closure-summary-then-dev-tensor transcript sample, and project health tests derive public module, Galaxy surface, and visible Galaxy library-module counts from the resolved project registry while retaining name-level contract assertions",
        next_step: "mirror the same closure-summary-then-dev-tensor reading order into any new project-status/build-report/package frontdoors as those surfaces grow",
        blocker: "the versioned primary frontdoor reading order and registry-derived surface summaries are status-visible, machine-readable, and regression-backed; future package/linker summary surfaces must keep consuming the same contracts",
        next_action: "keep future frontdoor/package/linker summary surfaces aligned to the closure_summary then dev_tensor_weakest_task_card_handoff reading order",
        validation_command: "cargo test -q -p nuis project_status && cargo test -q -p nuis workflow_surface && cargo test -q -p nuis --test status_frontdoor_smoke",
        expected_artifact: "workflow/project-status/project-doctor summary surfaces expose one shared closure_summary contract followed by the versioned nuis-frontdoor-reading-order-v1 handoff contract, project-status consumes link-plan/nsld closure evidence after build, docs define both closure_summary plus tensor task-card as the canonical human reading order, nuis status prints the stable closure-summary-then-tensor transcript sample, and project/workflow/status tests pin field presence, field order, documentation, runtime contract, and registry-derived surface counts",
    },
    DevTensorCell {
        architecture: "compiler-frontdoor",
        module: "nuis-cli",
        function: "artifact-runtime-closure",
        status: "stable",
        progress: 100,
        bootstrap_critical: true,
        closure_role: "artifact-execution-frontdoor",
        evidence: "run-artifact, workflow, artifact-doctor, project-status/project-doctor, build-report, release-check logs, release-check JSON summary renderer, release-check --json CLI entrypoint, command-level release-check --json stdout smoke, explicit runtime materialization policy fields, nsld handoff, self-contained host-runner probe fields, nsdb-ready launch_evidence contract fields, nsdb-facing payload execution trace records, persisted run-artifact nsdb payload handoff metadata, persisted hetero runtime trace metadata, generated payload decoder manifests, generated decoder manifest surface mirrors, nsdb inspect hetero runtime trace consumption, workflow/artifact-doctor persisted handoff mirrors, workflow/artifact-doctor launch-evidence mirrors, nsdb inspect payload handoff event consumption, nsdb event filters, dedicated nsdb events command, nsdb replay-plan checkpoints with stable frame/value anchors, value-sample references, trace-backed sample resolution status, value-sample materialization descriptors, value slot/schema metadata, typed value snapshot references, concrete metadata content summaries, opaque payload file summaries, decoder capability/detail-level boundaries, spec-driven lightweight payload format probes, external decoder manifest loading, ASCII/hex external magic probes, external decoder capability/detail declarations, decoder manifest diagnostics, inspect-level decoder manifest summaries, per-record decoder manifest diagnostics, decoder manifest protocol/schema version checks, and final-output boundary reports share closure evidence",
        next_step: "turn per-domain run-artifact trace records into real device execution samples without coupling release-check to runtime execution",
        blocker: "release-check now explicitly remains a pure readiness gate, so real runtime evidence must come from run-artifact/device sample lanes",
        next_action: "turn per-domain run-artifact trace records into real device execution samples without coupling release-check to runtime execution",
        validation_command: "cargo test -q -p nuis run_artifact && cargo test -q -p nsdb",
        expected_artifact: "workflow/artifact-doctor/project-status/project-doctor/build-report/release-check-summary/release-check-json/nsdb-facing surfaces report persisted debugger handoff/runtime trace metadata, generated payload decoder manifests, generated decoder manifest mirrors, and nsdb tooling consumes/filter/replay-plans it through inspect, events, replay-plan frame/value anchors, value-sample references, trace-backed sample-resolution status, materialized sample descriptors, value slot/schema metadata, typed value snapshot refs, value content summaries, opaque payload file summaries, decoder registry status, decoder capability/detail boundaries, spec-driven lightweight payload format probes, external decoder manifest loading, ASCII/hex external magic probes, external decoder capability/detail declarations, decoder manifest diagnostics, inspect-level decoder manifest summaries, per-record decoder manifest diagnostics, and decoder manifest protocol/schema version checks",
    },
    DevTensorCell {
        architecture: "linker-toolchain",
        module: "nsld",
        function: "artifact-chain-drive",
        status: "stable",
        progress: 100,
        bootstrap_critical: true,
        closure_role: "linker-artifact-chain",
        evidence: "nsld drive can apply whitelisted artifact-chain steps, stop cleanly on host-assisted final output boundaries, report mutation_policy, expose nsld-drive-safe-next-v1 gate fields, Nuis closure_summary promotes mutating drive recommendations into nsld-drive-safe-next next-action/command selection through the command-set safe_next probe, self-contained NSB smoke asserts the clean safe-next gate after until-clean, official shader/kernel hetero smoke asserts whitelisted drive apply plus rerun-drive safe-next guidance, nsld provider-sample boundary tests assert safe_next_contract/action/command/reason/gate fields through both apply and until-clean final-output boundary stops, and final-output object-package/replay plus closure summaries now mirror the shared artifact_chain_safe_next contract and probe command without giving replay mutation semantics",
        next_step: "keep future final-output, package, and replay consumers on the shared artifact_chain_safe_next mirror instead of introducing parallel linker transition vocabularies",
        blocker: "artifact-chain mutation, boundary gates, and package/replay return-to-driver probes are protocolized and regression-backed; future consumers must preserve the read-only replay versus mutating drive separation",
        next_action: "keep future package and replay consumers attached to the shared artifact_chain_safe_next contract and probe command",
        validation_command: "cargo test -q -p nuis workflow_surface && cargo test -q -p nuis project_status && cargo test -q -p nsld drive",
        expected_artifact: "nsld drive JSON reports mutation_policy, safe_next_action, safe_next_command, safe_next_reason, safe_next_contract, safe_next_gate_required, safe_next_gate_action, and crossing command fields; Nuis closure_summary recommends nsld-drive-safe-next plus the safe_next probe command whenever artifact-chain mutation is available; final-output object-package/replay and closure summaries expose artifact_chain_safe_next_contract plus artifact_chain_safe_next_probe_command; self-contained and official hetero smokes assert clean/rerun safe-next gates; provider-sample boundary drive tests assert safe-next gates after boundary materialization and explicit final-output crossing stops",
    },
    DevTensorCell {
        architecture: "linker-toolchain",
        module: "nsld",
        function: "final-output-boundary",
        status: "stable",
        progress: 100,
        bootstrap_critical: true,
        closure_role: "executable-output-boundary",
        evidence: "final-executable-output reports normalized boundary status, materialization status, recommended next action, path presence, ownership, runnable candidate, blockers, Nuis/Nsld mirrors, self-contained relocation-safe images, container-loader handoff readiness, first-payload execution metadata from the final output itself, nsdb-facing payload execution trace metadata, command-level persistence of the first-payload trace as an nsdb handoff record, nsdb replay-plan readiness, replay-evidence-ready status, checkpoint/replayable counts, stable replay next-action/next-command fields, Nuis frontdoor mirrors/closure selection for replay-ready or replay-blocked states, self-contained smoke asserts replay-ready contract/command fields, official hetero smoke asserts the replay-blocked resolve gate, and provider-sample materialization exposes nsld-final-output-boundary-return-v1 plus nsdb-payload-execution-replay-plan-v1. Nuis now captures those provider-neutral replay facts once as nuis-final-output-replay-vocabulary-v1; object-package and debugger-transcript surfaces receive complete contract/readiness/status/checkpoint/command/action/blocker projections from that typed source, with ready and blocked run-artifact regressions preventing drift",
        next_step: "keep future package-summary, debugger Galaxy, and closure APIs consuming the typed final-output replay vocabulary rather than reconstructing replay facts",
        blocker: "the final-output replay vocabulary and current package/debugger projections are protocolized and regression-backed; future consumers must preserve the typed single-source projection rule",
        next_action: "keep future package-summary and debugger Galaxy APIs attached to nuis-final-output-replay-vocabulary-v1",
        validation_command: "cargo test -q -p nuis link_plan_final_output_replay_vocabulary && cargo test -q -p nuis run_artifact_json_reports_ready_after_final_pipeline_exists && cargo test -q -p nuis run_artifact_json_blocks_missing_backend_payload_replay_gate && cargo test -q -p nsld final_executable_output && cargo test -q -p nsdb",
        expected_artifact: "final-output boundary emits trace-ready first-payload metadata, persists an nsdb handoff, exposes one typed nuis-final-output-replay-vocabulary-v1 source, and projects identical replay contract/readiness/status/checkpoint counts/commands/actions/blockers into object-package and debugger-transcript frontdoor fields without object-format or provider-specific branches",
    },
    DevTensorCell {
        architecture: "linker-toolchain",
        module: "nsld",
        function: "os-native-executable-finalization",
        status: "usable",
        progress: 92,
        bootstrap_critical: true,
        closure_role: "host-executable-finalization",
        evidence: "Nsld owns the deterministic NSB container/image route, final-output contracts, launcher manifests, architecture-specific native entry thunks, and nuis-nsld-executable-finalizer-registry-v1. native-cpu-llvm now compiles LLVM program and runtime shim inputs into separate relocatable objects, embeds them in the versioned NHOB host_objects_binary section, projects stable id/role/format/byte/hash evidence into artifact inspect/verify and LinkPlan reports, and restores the complete object handoff from a post-manifest compile cache snapshot. The Mach-O arm64 internal provider consumes this compiled-artifact-native-handoff, requires exactly one program-llvm and runtime-shim role, verifies plan identity and content hashes, parses each object as arm64 MH_OBJECT with LC_SEGMENT_64, then validates and atomically materializes the current compatibility MH_EXECUTE image without a second Nsld-side clang invocation. Missing, malformed, duplicate, drifted, or stale-cache object handoffs fail closed. The host-command provider remains gated; ELF and PE/COFF remain explicit registered-not-implemented providers. Binary protocol, real clang/cache roundtrip, Mach-O parser, and runnable finalizer CLI regressions pass.",
        next_step: "teach the Mach-O provider to consume the verified host object symbol and relocation tables and emit the executable shell itself, then add equivalent ELF and PE/COFF providers behind the same registry",
        blocker: "Nsld validates real relocatable inputs but still materializes the compatibility executable prelinked by Nuisc; Mach-O symbol resolution, relocation application, load-command synthesis, and executable-shell layout are not yet owned by Nsld, while ELF and PE/COFF finalization remain incomplete",
        next_action: "replace prelinked-image materialization with Nsld-owned Mach-O arm64 symbol resolution, relocation application, and executable-shell emission using the verified program/runtime object handoff",
        validation_command: "CARGO_INCREMENTAL=0 cargo test -q -j 1 -p nsld",
        expected_artifact: "one verified open finalizer registry consumes an NHOB-bound pair of real program/runtime Mach-O objects with identity/hash/structure checks, atomically installs the current runnable compatibility image without a second Nsld-side host link, retains an explicitly gated host fallback, and keeps Nsld-owned relocation plus ELF/PE-COFF limitations visible",
    },
    DevTensorCell {
        architecture: "heterogeneous-runtime",
        module: "nustar",
        function: "registered-domain-contracts",
        status: "stable",
        progress: 100,
        bootstrap_critical: true,
        closure_role: "heterogeneous-domain-registration",
        evidence: "domain registration, backend readiness, dispatch/link-plan consumption, Nsld assembly, final-layout payloads, frontdoor mirrors, provider registry/probes, fallback and native execution are connected. Seven ordered requests use five CoreML nodes and two Metal nodes; the cross-provider chain compares [11,17,23,29], the KMeans centroid scorer compares [0,16], and a generic Metal argmax emits exact u32 assignment 1, all with zero mismatch. Native outputs can use sealed NUISPFD1 carriers or an honest host-owned fallback. Dependency edges preserve producer storage through provider.output.transfer.v1 when a transferable carrier exists and otherwise record memory-owned staging without inventing device residency. CoreML configures independent transferable input channels, including two-input fan-in. Unix output verification returns a read-only mmap-backed ProviderOutputPayload shared by comparison, summaries, hashes, and completed-output metadata instead of retaining a duplicate Vec; portable hex output retains an owned fallback. Writable single-frame carriers emit fixed metadata plus a sparse payload span. Output residency and session contracts record honest host/device capability, deterministic leases, lifecycle hooks, GLM-owned handles, and graph-close release. Worker transports prove one persistent stdio PID and frame-bound SCM_RIGHTS ownership. UnixWorkerProcessTransport inherits only the child socket endpoint, validates a PID handshake against Child::id, preserves sparse original input role indices, detects worker exit while awaiting a reply, and sends post-spawn descriptors to the same compiled worker process. NUISPWU2 adds a hash-checked opaque binary request and one ordered semantic role per descriptor. NUISPWUR7 adds request length/hash identity without body echo, adapter protocol length/hash metadata, and ordered worker-owned output descriptor manifests, binds the original request hash and input/output role manifests, and carries the positive Nuis invocation status; decoding rejects non-positive status, mismatched ancillary count, unreadable payload, and output hash drift. The std provider-worker lifecycle and dispatch registry are authored in Nuis. ProviderWorkerRequest carries opaque request and descriptor-table handles; ProviderDispatchBinding supplies an open-ended provider key, dispatch token, capability hash, and enabled state. The registered `provider_request_ingress` intrinsic preserves its five-scalar compatibility form and adds an eight-scalar capsule form carrying token plus input/output role counts. Data Nustar interpretation returns only the opaque request handle, GLM records five or eight value reads, and LLVM performs scalar passthrough while preserving all metadata in YIR. The persistent Nuis worker reads capsule scalars from its verified frame, validates token and role counts, emits eight explicit ingress dependencies, invokes a registry-derived provider-neutral capsule invoker, and only then replies. The thin Unix ABI adapter creates negotiated anonymous output descriptors and transfers them with SCM_RIGHTS; Nsdb independently verifies every role, length, and FNV hash before publishing worker-invoked evidence. Process-adapter v5 supplies open ordered literal, verified-path, descriptor-path, and descriptor-carrier argument templates. Registered PixelMagic gray8, WitSage Metal f32 bias/argmax, and WitSage large, chained, two-input fan-in, and KMeans centroid-score CoreML requests execute beneath persistent Nuis workers with hash-verified executables and model control assets; every worker result returns as a sealed transferable NUISPFD1 carrier. `nuis-provider-process-adapter-cache-v1` derives an open identity from source, runner contract, ordered frameworks, OS, and architecture; one graph owns immutable images, five CoreML requests prove `compiled,hit,hit,hit,hit`, the two Metal contracts own independent compiled identities, and graph teardown removes all temporary files. `nuis-provider-worker-adapter-control-v2` compacts the fixed launch header and ordered open arguments into one request-hash-bound record and enforces a portable 1800-byte budget before sendmsg. `nuis-provider-worker-adapter-control-carrier-v1` spills manifests above the 384-byte inline budget into an unlinked length/hash-bound `control.adapter` descriptor; the v27 worker requires that role to be unique and last, excludes it from capsule input counts and input byte sums, and real CoreML reports carrier mode while preserving two-input fan-in. `nuis-provider-output-binding-v1` maps ordered roles to output buffers, ProviderWorkerLease retains every verified result, and provider sessions allocate one GLM-owned output handle per role while preserving the first output as the compatibility primary. `nuis-provider-graph-output-ownership-v1` indexes completion by request plus output buffer, converts additional lease results into registered graph outputs, routes two distinct outputs to distinct consumers, and reports all released roles at graph close. `nuis-provider-execution-adapter-registry-v1` moves Native, Metal, and CoreML request validation, worker preparation, and result interpretation into independent registration providers; the provider-sample frontdoor now invokes only the selected registration and contains no concrete backend or operation branch. `nuis-provider-bundle-registry-v1` gives every provider one static bundle that cross-binds its unique bundle ID, runner profile, and Unix execution adapter; both selectors consume this shared AOT membership list and reject invalid nested contracts. `nuis-provider-bundle-manifest-v1` is now generated from versioned `provider_bundles` entries owned by the registered Data, Shader, and Kernel Nustar manifests. Nuisc validates entry syntax and registry-wide uniqueness, `.nustar` binary manifests preserve the field, and the Nsdb build stage emits one bundle-ID-sorted static table plus canonical FNV hash. Both selectors fail closed unless generated count, ordering, hash, package provenance, IDs, families, runner adapters, execution kinds, and Rust static bindings agree. Nuis independently parses and validates the provider-neutral registry contract, manifest contract/hash/count, and opaque package/bundle IDs before exposing them through device-sample final output, object-package audit aliases, artifact-doctor mirrors, and frontdoor closure JSON. `identity.selected-provider-bundle-set` is the first consumer of Nsld's open `metadata_binding` table: its verified contract/count/hash participates in both the metadata table root and the complete container hash, while disagreement blocks container emission before either metadata or payload files are written. Pending and blocked execution samples can still carry a verified immutable identity through drive diagnostics without conflating selection integrity with execution completion. The final NSB image embeds the canonical container bytes. Nsld's container loader and `nuis-host-runner` independently extract the binding table from final-image payload bytes, recompute its table hash, reject count/hash/duplicate/required-validation drift, validate the selected-set shape, and block lifecycle handoff on mutation even when the outer NSB hash is refreshed. Host-runner JSON/text expose the observed count/table hash/validation/selected-set identity, and Nuis launch evidence requires either an explicit empty not-applicable proof or a verified selected-set proof before reporting ready. Both run-artifact and direct Nsld final-output writers now pass those provider-neutral values through the Nsdb-owned final-image binding claim API. Nsdb computes and persists `nuis-final-image-binding-proof-v1`, rejects conflicting final-image claims, preserves the proof across provider completion merges, and mirrors verified status through replay and Nsld final-output summaries. Proof-less handoffs remain inspectable as `legacy-unbound`, but public replay summaries, actual Nsdb replay plans/transcripts, and Nuis's independent final-output closure now block them with `rebuild-final-output-binding-proof`; provider completions that arrive before final-image evidence remain a valid mergeable but non-replayable lifecycle intermediate. The sealed container additionally embeds `nuis-final-image-provider-dispatch-v1`, an open ordered package/bundle/family/runner/adapter table required by `runtime.provider-dispatch-table`; Nsld recomputes it from actual final bytes and rejects selected-set, binding, adapter, count, or hash drift. Host runner independently exposes and verifies every entry. Nsdb reports launcher-less work as pre-seal acquisition, but once a final image exists it requires sidecar package/bundle/family and actual runner/adapter identity plus the registered runtime adapter to match that immutable table before worker launch; the official CoreML/Metal route proves post-seal execution authorized 2/2 selected bundles. Cursor lineage v2 and repair journal v6 independently retain the verified provider dispatch identity across bounded generations and repair ancestry, while Nuis final-output text/JSON and closure JSON expose only the identity accepted by that mirror. Nuis validates handles and registration identity, rejects mismatched capability or capsule metadata, advances ordered accept/reject/commit state, and AOT-executes lifecycle, ingress, invocation, and reply control flow. C and Objective-C runners remain bootstrap ABI probes or concrete backend adapters rather than owners of worker control flow",
        next_step: "keep the validated dispatch capability and official.cffi source boundary stable while future package-summary, debugger Galaxy, and provider-neutral Data hardware adapters such as DPU/IPU consume them through typed Nuis interfaces",
        blocker: "none for the current core capability and frontdoor projections; concrete package-summary/debugger Galaxy libraries remain future consumers rather than authority owners",
        next_action: "advance to the next bootstrap-critical coordinate while preserving one lineage-derived provider dispatch identity source and the strict mod cffi host boundary",
        validation_command: "cargo test -q -p yir-domain-cffi && cargo test -q -p nuisc --test ffi_compile && cargo test -q -p nuis-host-runner && cargo test -q -p nsdb && cargo test -q -p nsld && cargo test -q -p nuis --test official_galaxy_hetero_smoke && cargo test -q -p nuis dev_tensor",
        expected_artifact: "package-summary and debugger API final-output/closure fields share nuis-validated-provider-dispatch-identity-capability-v1, official.cffi owns exact-whitelisted extern source admission while registered CPU lowering owns current host machine-code realization, CPU-only identity remains verified-empty, and invalid source or provider lineage blocks",
    },
    DevTensorCell {
        architecture: "heterogeneous-runtime",
        module: "data",
        function: "provider-neutral-data-fabric",
        status: "early",
        progress: 32,
        bootstrap_critical: false,
        closure_role: "open-data-hardware-provider",
        evidence: "Data YIR, GLM ownership, clocked movement contracts, provider registration, CPU/device-memory staging, descriptor transfer, and cross-provider graph edges are established. DPU, IPU, RDMA, and future data-fabric devices have an architectural registration slot, but no native DPU/IPU inventory, code artifact, execution adapter, or physical completion evidence is checked in yet.",
        next_step: "turn the Data Nustar direction into a capability-ranked provider contract that can describe CPU memory, device memory, RDMA, DPU, and IPU without choosing a vendor in compiler code",
        blocker: "there is no available native DPU/IPU backend or hardware completion lane; current evidence stops at provider-neutral movement and ownership contracts",
        next_action: "add a versioned Data provider inventory/capability record plus a deterministic CPU-memory reference provider before attaching the first physical DPU or IPU adapter",
        validation_command: "CARGO_INCREMENTAL=0 cargo test -q -p yir-domain-data && CARGO_INCREMENTAL=0 cargo test -q -p nsdb provider_sample && CARGO_INCREMENTAL=0 cargo test -q -p nuis --test official_galaxy_hetero_smoke",
        expected_artifact: "the registry can select and audit an open Data provider from capability records, execute the CPU-memory reference path, and preserve GLM, clock, residency, movement, and completion evidence without naming a finite DPU/IPU vendor set",
    },
    DevTensorCell {
        architecture: "heterogeneous-runtime",
        module: "linux-cuda",
        function: "cuda-provider-bringup",
        status: "stable",
        progress: 100,
        bootstrap_critical: false,
        closure_role: "cross-platform-gpu-provider",
        evidence: "nuis-linux-cuda-host-probe-v1 and a real x86_64 RTX 4050 establish CUDA 12.0, sm_89, hash-bound nuis-cuda-device-inventory-v1 evidence, and capability-ranked-lowest-ordinal selection. Kernel Nustar owns cuda.nvidia-gpu.bundle.v1, nuis-kernel-code-asset-registry-v1, and the project-derived PTX emitter. Verified YIR function membership feeds nuis-kernel-yir-codegen-table-v1 and nuis-kernel-yir-source-adapter-v1. add_scalar_axis becomes an i64 map entry with nuis-kernel-source-request-projection-v1; reduce_sum_axis becomes a dependent i64 reduction entry with explicit provider edge, GLM ownership, and clock evidence. element_at(1x1,0,0) becomes nuis-kernel-source-result-projection-v1 without generating another compute entry. Complete remote build, CUDA Driver execution, materialization, rebuild, Nsld seal, post-seal execution, and replay select a self-contained two-request project collection. The project codegen table binds its function entries exactly to generated request projections and its PTX contains only map and reduction entries. nuis-kernel-project-code-asset-identity-v1 hashes the source FNV, lowering target, entry count, and ordered entry set into a deterministic `kernel.cuda.project.<hash>` ID. Nuis independently recomputes every sidecar field before projecting collection-level identity evidence. Nsdb now independently recomputes that evidence before worker launch, requires ordered request entries and all code-asset descriptors to share ID, bytes, format, target, path, and digest metadata, rejects project IDs without the identity contract, and fails closed on source hash, entry order, identity hash, or per-request ID drift. The verified identity becomes typed collection state, is serialized once into provider output, validated into completion evidence, participates in the completion record hash, and reaches Nsld final-output replay as the same contract, status, asset ID, and identity hash. nuis-provider-code-asset-identity-set-v1 now forms an ordered provider-neutral root over `(asset_id, identity_contract, identity_hash)` entries. Nuisc emits the set sidecar, Nuis independently recomputes it, Nsdb validates membership and order before launch, completion hashing binds contract/status/count/root, and Nsld replay preserves the exact root. The CUDA graph proves the count-one project base case without changing its backend-owned ID; registration-only and non-project outputs explicitly report not-applicable/count-zero rather than inventing lineage. nuis-nustar-code-asset-identity-contribution-v1 now lets provider-bundle-owned contributions enter a deterministic request-index-ordered assembler without naming backend combinations. The real WitSage CoreML/Metal project contributes two Shader-owned MSL assets, persists and byte-validates both, emits a count-two descriptor identity set, executes the mixed graph on M2, and preserves its root through completion and sealed-image Nsld replay. nuis-nustar-code-asset-registration-v1 now places both MSL sources, output paths, entries, targets, and ownership in the official Shader manifest. Source and binary manifests preserve the open registrations; registry validation checks syntax, declared lowering targets, source availability, and registry-wide asset/path uniqueness. Galaxy manifests can now declare provider-neutral cross-domain code asset requirements. Nuisc resolves them through the open Nustar registry, persists them in the build context, materializes only the exact registered assets not already emitted by domain units, and rebuild/relocation preserves the same requirement set. This lets the Kernel-only WitSage Galaxy obtain its two Shader assets without inventing a Shader execution unit or hard-coding a backend combination. Nuisc emits nuis-domain-code-asset-contribution-table-v1 directly from the actual assets written during domain artifact assembly. The table is deterministically sorted by domain/package/asset, carries independent Nustar ownership plus byte, entry, lowering-target, descriptor identity, set-root, and table hashes, and is already included in normal build-manifest artifact hashing. Project PTX rows retain the semantic `kernel.cuda.project.<hash>` asset ID while their independent descriptor identity binds physical bytes. Nuis parses the table independently, re-reads every compiled asset, recomputes every descriptor identity, set root, and table hash, fails closed on mutation, and emits either one-row or ordered multi-row contribution selection evidence. The count-two selection set follows request semantics rather than physical table order: WitSage selects vector-bias table row 1 before argmax row 0. Nsdb independently repeats whole-table, every selected-row, actual-byte, ordered-request, entry, and uniqueness validation before creating any session, worker, or adapter. Metal receives the verified path and entry and loads the exact registered MSL instead of embedding source. CompiledCodeAssetSelectionEvidence is typed collection state, is serialized beside semantic identity, revalidated from provider output, persisted through handoff, included in completion record/set hashes, and independently shape-validated by Nsld before replay readiness. A real seven-node M2 graph with five CoreML and two Metal requests completed successfully through this path. Registration-only artifacts retain and validate `kernel.vector-arithmetic.f32.cuda.ptx` without requiring project identity. The CUDA map returns [11,12,13,14] with 0xbdf4a47186386b21; the dependent reduction and projected host-visible scalar both return 50 with 0xf71115b38f042bf7. Outputs receive completion and GLM-release evidence beneath immutable final-image binding and dispatch authority. The closure requires no nvcc, CUDA headers, SDK linkage, or runtime ptxas. After the ordered multi-row contribution upgrade, the dedicated Linux role again completed the full current-worktree build, pre-seal execution, materialization, rebuild, Nsld seal, post-seal CUDA Driver execution, and completion replay on the RTX 4050",
        next_step: "keep the completed Linux CUDA closure regression-stable while new Shader and Kernel backends reuse the provider-neutral Galaxy code-asset requirement and contribution-selection protocols",
        blocker: "none for the current Linux CUDA provider closure; broader NVIDIA ISA coverage and additional GPU families remain later backend expansion work",
        next_action: "advance to the next incomplete development-tensor coordinate while preserving remote CUDA closure coverage",
        validation_command: "bash -n scripts/probe-linux-cuda-host.sh && cargo test -q -p nsdb && cargo test -q -p nsld && cargo test -q -p nuis --test official_galaxy_hetero_smoke official_galaxy_hetero_projects_emit_shader_and_kernel_artifacts -- --nocapture && cargo test -q -p nuis --test official_galaxy_hetero_smoke sealed_linux_cuda_image_executes_and_replays_completion -- --nocapture && cargo test -q -p nuis dev_tensor",
        expected_artifact: "Metal consumes two ordered table-selected MSL rows without built-in shader source, while the remote CUDA closure still proves identical compiler table hash/root across provider input, real-device output, signed completion lineage, and Nsld replay",
    },
    DevTensorCell {
        architecture: "heterogeneous-runtime",
        module: "linux-vulkan",
        function: "vulkan-provider-bringup",
        status: "stable",
        progress: 100,
        bootstrap_critical: false,
        closure_role: "cross-vendor-gpu-provider",
        evidence: "Shader Nustar statically registers spirv.vulkan-gpu.bundle.v1 for spirv:vulkan-gpu. Nsdb owns nuis-vulkan-host-probe-v1, loads libvulkan.so.1 directly without Vulkan SDK tools or headers, resolves the instance API, creates an instance, and requires at least one physical device. The hard-gated probe passed on a real x86_64 RTX 4050 host. The official Shader manifest now owns shader.vulkan.copy-u32.spirv through nuis-nustar-code-asset-registration-v1, and Nuisc lowers its UTF-8 inline WGSL copy-u32 body from a .ns code-asset source into deterministic SPIR-V 1.6 bytes without glslang, SDK headers, runtime shader strings, or standalone .nspv source files. AOT materialization writes nuis.shader.vulkan.copy-u32.spv, emits the normal domain code-asset contribution table row, and verifies asset ID, format, lowering target, target, entry, and path. Nuis registers an official.shader device-sample projection for backend vulkan/discrete-or-integrated-gpu, but canonical provider-family resolution now comes from the registration contract so the handoff target is spirv:vulkan-gpu instead of a guessed backend/device pair. Vulkan code-asset selection uses the same contribution selection-set protocol as multi-asset graphs, including the count-one base case, and the WitSage resolver now only owns its own two Shader assets rather than reprocessing unrelated Shader registrations. The Linux CUDA/Vulkan host has spirv-tools and Vulkan tools available; an opt-in NUIS_SPIRV_VAL validation run sent the generated module to remote spirv-val and proved the registry-owned SPIR-V is accepted for Vulkan 1.3 semantics. Nsdb's Vulkan execution adapter owns nuis-vulkan-spirv-execution-session-plan-v1, verifies provider family, adapter binding, code asset descriptor, SPIR-V header/hash/entry, u32 input/output ABI, element_count, and 4x1x1 dispatch, then prepares a registered C process adapter only on Linux. A checked-in shader_vulkan_provider_demo now builds a Linux x86_64 Vulkan shader artifact, run-artifact materializes the official.shader provider sample, execute-provider-samples runs the registered Vulkan worker through the RTX 4050, and materialize-provider-samples attaches completion evidence. The output payload records nuis-vulkan-spirv-provider-execution-v1, vulkan-compute-dispatch-completed, selected device evidence, verified compiled code-asset selection, comparison-passed output, and hash 0x6ebcdd244b594ee4. Nsld now accepts the count-one selection-set base case in final-output code-asset validation, embeds spirv:vulkan-gpu in the provider-neutral final-image dispatch table, and rejects runner adapter drift from final bytes. Nsdb binds Vulkan provider completions to the immutable final-image dispatch authority. Nuis object-package/debugger/frontdoor surfaces now accept the verified final-output dispatch identity as a provider-neutral projection source when a single-checkpoint replay has no resumable cursor lineage, while still failing closed for invalid lineage. The remote Linux Vulkan smoke rebuilds the provider-complete graph, runs nsld seal, verifies post-seal dispatch authorization, and reports replay-ready Nsld/Nuis final-output provider completion evidence. Nuisc now summarizes normalized inline WGSL into nuis-inline-wgsl-summary-v1 stage/binding facts before shader project validation, so comments cannot spoof stage availability and vertex, fragment, compute, workgroup_size, return type, texture, sampler, uniform, and storage facts have a common frontend extraction point. YIR lowering contracts now slice inline WGSL by ordered vertex/fragment/compute markers and emit a compute shader_ir stage with an explicit end/void terminator. The same contract now exposes nuis-yir.shader.module-summary.v1 through text and package manifests, including source language, resource, entry, stage count, binding count, stage entries, workgroup size, return type, and group/binding/type facts. That module summary now feeds nuis-yir.shader.backend-lowering-plan.v1 entries for spirv:vulkan-gpu, msl:metal-gpu, dxil:directx-gpu, glsl:opengl-gpu, and host-simd:cpu-fallback, including native IR target, source schema, stage execution model, target entry, and resource binding slot mappings without adding backend-specific linker branches. Nuisc AOT shader sidecars now declare the same module-lowering-plan contract, source schema, lowering boundary, profile-to-provider target mapping, accepted stage kinds, execution models, binding slot model, and target entry policy for Metal, Vulkan, DirectX, OpenGL, and host fallback profiles. The registry now extracts a single `wgsl { ... }` block from canonical .ns code-asset sources before invoking the shared SPIR-V/MSL emitters, so official u32 Shader registrations are Nuis-source-owned rather than naked backend-source-owned. The same canonical body contract now feeds two backend emitters: the shared canonical compute module owns copy/add/sub/mul/xor/add-pair u32 operation recognition, SPIR-V maps it to native integer bytecode, MSL validates the same module plan shape, and both preserve ordered storage bindings. The official Shader manifest registers canonical Metal and Vulkan u32 assets from assets/shader/*.ns sources, so AOT can materialize proof-bearing MSL/SPIR-V without treating backend text as official source. Nsdb derives Vulkan input/output counts from verified SPIR-V DescriptorSet, Binding, NonWritable, and NonReadable decorations instead of a finite entry table. The official.shader.vulkan-add-pair-u32 sample materializes two independent artifact bindings, routes both descriptors through the generic worker adapter, and executes on the RTX 4050 with comparison-passed hash 0xbada6f73928b9f42. Canonical Shader outputs are now an ordered one-to-eight resource list in both MSL and SPIR-V. The SPIR-V emitter allocates IDs per output, Nsdb reflects ordered writable bindings, and the split thin Vulkan runner dynamically creates and persists every descriptor supplied by the provider-neutral worker manifest. The official add+xor pair node completed one RTX 4050 Vulkan dispatch with sum hash 0xbada6f73928b9f42 and xor semantic hash 0x73bb5b39fe3ab738, both comparisons passed, and the result survived GLM release, NSLD seal, and final-image frontdoor projection. Vulkan output plans now preserve per-binding logical bytes, carrier bytes, row bytes, stride, and row count. A second real RTX dispatch kept the 16-byte sum output while staging xor into a 24-byte stride-12 carrier with semantic hash 0x9adad3c97291d1e8; comparison collection, GLM release, NSLD seal, and final-image projection all passed without adding an operation-specific backend branch. Existing Metal/Vulkan scalar samples and add-to-xor chains continue to preserve provider-neutral selection, GLM transport, final-image replay, and linker surfaces. The official reduced Vulkan fan-out graph now routes output.xor into a second copy-u32 request through a typed v2 dependency binding with shape 2x1, row stride 8, byte length 8, dispatch 2x1x1, and element_count 2. A real RTX 4050 run preserved hash 0x279d73758e81abdd and the GLM transport token through comparison, compiled selection count two, NSLD seal, replay, and final-image frontdoor closure without a Vulkan-specific graph branch. The graph now expands into one producer plus two independent consumers with unique reduced-fan-out, copy, and xor assets. A real RTX 4050 run preserved the 2x2 16-byte sum edge and the 2x1 8-byte xor edge through request-1 and request-2 clocks, emitted two GLM transport receipts and three compiled selections, completed all three request-level comparison results including the producer's two-item collection, and sealed the final 8-byte zero output at hash 0xa8c7f832281a39c5 through NSLD replay and final-image closure. Project manifests now expose an open `code_assets` requirement set rather than forcing cross-Nustar assets through an unrelated Galaxy. Kernel Nustar registers `kernel.cuda.copy-u32.ptx` from a canonical UTF-8 .ns declaration, filters the registered Kernel/YIR table to `nuis_kernel_copy_u32`, and emits typed u32 PTX without embedding backend source. The official Vulkan/CUDA fan-out record carries three requests and three independently owned contributions: reduced SPIR-V producer, CUDA PTX sum consumer, and Vulkan SPIR-V xor consumer. A real RTX 4050 run completed all three adapters, preserved the 2x2/16-byte and 2x1/8-byte dependency bindings, both GLM clocked receipts, comparison closure, count-three selection set, CUDA and Vulkan execution contracts, NSLD seal/replay, and final-image frontdoor. The old all-Vulkan fan-out and project-derived CUDA closures passed again after the expansion. Nsdb now derives the first-occurrence-ordered provider-family set from every request adapter binding, resolves each family through the open static bundle registry, and materializes a provider-neutral dispatch table beside the compatibility sample record. Nsld prefers that table, binds both count and root into final-image metadata and runtime dispatch authority, and retains record parsing only for legacy images. A second real RTX 4050 run sealed Vulkan plus CUDA as two independently registered dispatches, reauthorized both request families after seal, and projected the verified two-bundle root through replay, object-package, debugger, closure summary, and launch binding without naming the backend pair in compiler or linker code. `nuis-provider-request-completion-receipt-collection-v1` now derives one ordered receipt from every native output, binding request ID, provider family, sealed dispatch ID, completion clock, output hash, completion token, and the shared selected-set root. Nsdb rejects duplicate request IDs, missing dispatch families, family drift, and root mismatch, includes the collection in completion integrity, and exposes verified contract/count/root through replay. A real RTX 4050 mixed run bound Vulkan, CUDA, and Vulkan requests to dispatch0000, dispatch0001, and dispatch0000; Nuis independently recomputed the receipt root and completion digest before allowing its frontdoor, while the all-Vulkan fan-out and standalone CUDA sealed closures passed unchanged. `nsdb-provider-request-replay-selector-v1` resolves an exact verified request ID to source trace, provider family, sealed dispatch, completion clock, output hash, completion token, selected-set root, and its deterministic `nsdb-provider-request-replay-frame-v1` child frame. Request frames retain signed receipt order beneath their provider-completion parent, bind ordinal and clock into SHA-256 frame identity, and preserve old checkpoint counts while adding explicit frame/request-frame counts. Local expansion, selection, replay, and cursor regressions prove that selecting a middle request stops on its child frame and advertises the exact next request frame; missing, ambiguous, malformed, or unverified evidence remains fail-closed. After the transient SSH outage cleared, the real RTX 4050 Vulkan/CUDA/Vulkan smoke rebuilt the current worktree, selected the CUDA middle child frame, persisted its cursor, and resumed exactly one frame into the final Vulkan request. The Nuis-only frontdoor now forwards `--save-cursor`, atomically records `nuis-debugger-cursor-selection-v1`, follows a custom cursor path from build-report and closure projections, rejects malformed selection records without falling back to a stale default, and resumes the exact signed Vulkan successor frame. The real mixed RTX 4050 smoke passed this Nuis debug-request, projection, and debug-resume path without direct Nsdb cursor persistence.",
        next_step: "keep the completed Nuis-only request-frame stop, cursor selection, projection, and resume boundary regression-stable while future heterogeneous providers reuse the same debugger contracts",
        blocker: "none for the current Linux Vulkan/CUDA/Vulkan debugger closure; additional provider families remain future registrations rather than blockers for this completed lane",
        next_action: "route future provider request frames through the generic request replay and Nuis cursor-selection contracts without adding backend-specific debugger branches",
        validation_command: "CARGO_INCREMENTAL=0 cargo check -q -p nuisc -p nsdb -p nsld -p nuis && CARGO_INCREMENTAL=0 cargo test -q -p nsdb provider_request_completion && CARGO_INCREMENTAL=0 cargo test -q -p nsdb replay && CARGO_INCREMENTAL=0 cargo test -q -p nsdb request_replay_frames && CARGO_INCREMENTAL=0 cargo test -q -p nsdb request_stop_cursor_targets_exact_successor && CARGO_INCREMENTAL=0 cargo test -q -p nuis parses_debug_request_selector && CARGO_INCREMENTAL=0 cargo test -q -p nuis artifact_nsdb_handoff_request_completion && CARGO_INCREMENTAL=0 cargo test -q -p nuis --test official_galaxy_hetero_smoke linux_vulkan::linux_vulkan_producer_fans_out_to_cuda_and_vulkan_consumers -- --exact --nocapture && CARGO_INCREMENTAL=0 cargo test -q -p nuis dev_tensor",
        expected_artifact: "`nuis debug-request --request-id <id> --save-cursor <path>` stops on the exact signed child frame, atomically selects the custom cursor for Nuis build-report/closure projection, and `nuis debug-resume` consumes only its verified successor without importing provider-specific logic",
    },
    DevTensorCell {
        architecture: "standard-library",
        module: "std",
        function: "host-io-filesystem-text",
        status: "stable",
        progress: 100,
        bootstrap_critical: true,
        closure_role: "bootstrap-std-foundation",
        evidence: "std IO/filesystem/text/stdin examples, std_filesystem_smoke light lane, observable CLI smoke, direct cli_wc_demo argv/file/text-stats run, direct cli_pgm_invert_demo image-shaped file read/write run, reusable StdReportContracts, direct cli_report_file_demo argv/file/text report output, PixelMagic report-file consumption of std-preprocessed PGM input, PixelMagic/WitSage report-file workloads, official_galaxy_hetero_smoke shader/kernel artifact smokes, direct run-artifact hetero trace record assertions, explicit nsdb provider-sample materialization, deterministic provider-sample artifact files with hash evidence, and registered nuis-provider-runner-adapter-v1 fallback descriptors. The std boundary now ends at host preprocessing evidence: nuis-device-sample-input-registration-v1 dispatches subsequent image-buffer construction to the AOT registration table, and the nuis.pixelmagic registration exclusively owns gray8 shape, payload, kernel, hash, and persistence metadata before execute-provider-samples validates, uploads, inverts, and reads back that buffer through a real Metal command buffer on supported Apple hosts while preserving deterministic fallback elsewhere",
        next_step: "keep the completed std host IO/filesystem/text-to-PixelMagic payload boundary regression-stable while image execution evolves in the PixelMagic and heterogeneous-runtime cells",
        blocker: "none for the current bootstrap std scope; richer image codecs and asynchronous IO remain later capability work rather than closure blockers",
        next_action: "route subsequent image-buffer generalization through PixelMagic/provider contracts without widening the completed std bootstrap boundary",
        validation_command: "cargo test -q -p nuis --test std_filesystem_smoke && cargo test -q -p nuis --test official_galaxy_hetero_smoke",
        expected_artifact: "Std observable CLI smoke covers stdin, wc-style file/text stats, PGM image-shaped file transformation, report-file generation, PixelMagic std-preprocessed PGM input consumption, PixelMagic/WitSage report workloads, hetero domain artifacts, run-artifact trace records, materialized provider-sample manifests, shape/hash-bound raw gray8 payload persistence, provider runner adapter contract metadata, deterministic fallback evidence, and real Metal buffer upload/invert/readback output bytes/hash/device evidence on supported macOS hosts",
    },
    DevTensorCell {
        architecture: "standard-library",
        module: "std",
        function: "concurrency-task-thread-lock",
        status: "stable",
        progress: 100,
        bootstrap_critical: true,
        closure_role: "bootstrap-concurrency-runtime",
        evidence: "Nuis source compiles task, thread, mutex, timeout, cancellation, join-result, and observer flows while GLM rejects moved thread/guard/permit reuse. Structured if/match selected-prefix lowering emits mutex and task chains exactly once. Mutex handles remain opaque, monotonic, generation-bound, and acquire/release ordered. SharedMutex preserves linear ownership, literal 1..=64 permit cardinality, one-shot static lanes, explicit close/revocation, lease mutation, stale/replay rejection, and one central release-published value across interpreter and native runtime. The payload contract is scalar-i32-i64-native-staged-fallback-v1: YIR helper parameter types preserve MutexPermit<T>, LLVM wraps permit tokens with their scalar kind, task contexts transport only opaque handle bits, and helper entry reconstructs the typed permit. The scalar runtime stores and checks an explicit kind tag; signed i32 values are sign-extended into the slot and truncated on read/replace without becoming source-visible handles. A scheduler-private C11 atomic admission gate now serializes every mutex, guard, and permit table lookup, allocation, mutation, observer, and reset; nested permit-lock and lease-unlock paths use already-admitted internal operations without recursive gate acquisition. A 32-pthread harness simultaneously allocates distinct mutex handles, shares and consumes one permit per handle, holds 32 live guards, verifies unique identities and payloads, then concurrently unlocks/closes every slot with exact counters and zero live residue. task_shared_mutex_i32_demo still sends two MutexPermit<i32> values through native tasks, replaces 17 with 23, observes 23 through scalar-v1 ABI calls without deferred mutex lowering, and exits 63. Existing i64 permit, cardinality, replacement, branch, close, interpreter, C harness, and native binary regressions remain green.",
        next_step: "keep the completed scheduler-private host-thread admission boundary regression-stable while a future executor and richer payload/cardinality work evolve behind the same opaque authority contracts",
        blocker: "none for the current beta foundation slot-synchronization scope; a mature parallel executor, runtime-dynamic cardinality, per-mutex parking/fairness, and bool/float native payloads remain later work",
        next_action: "keep the closed mutex admission and scalar ABI regression-stable while the bootstrap selector advances linker-toolchain/nsld/os-native-executable-finalization",
        validation_command: "CARGO_INCREMENTAL=0 cargo test -q -p yir-domain-cpu tests_mutex_lifecycle && CARGO_INCREMENTAL=0 cargo test -q -p yir-lower-llvm && CARGO_INCREMENTAL=0 cargo test -q -p nuisc aot::tests::shim_mutex --lib && CARGO_INCREMENTAL=0 cargo test -q -p nuisc --test mutex_permit_glm_verify && CARGO_INCREMENTAL=0 cargo test -q -p nuisc --test thread_mutex_branch_compile && CARGO_INCREMENTAL=0 cargo test -q -p nuis --test concurrency_branch_native_smoke",
        expected_artifact: "32 simultaneous host workers contend on scheduler-owned mutex/guard/permit admission without aliases or data races while i32/i64 payload typing, static cardinality, canonical metadata, exactly-once branch lowering, explicit close/revocation, and stale/duplicate/out-of-range/replay rejection remain intact",
    },
    DevTensorCell {
        architecture: "standard-library",
        module: "pixelmagic",
        function: "image-processing-lane",
        status: "stable",
        progress: 100,
        bootstrap_critical: false,
        closure_role: "official-compute-package",
        evidence: "PixelMagic has official package contracts, shader surfaces, domain demos, a std-report-file workload, checked shader pipeline builds that emit Metal sidecar/payload artifacts, direct run-artifact assertions for shader hetero runtime trace records, provider-scoped sample validation descriptors, and explicit contract.pixelmagic.provider-sample-input-registration.v1 plus contract.pixelmagic.filter-plan.v1 package surfaces. The checked-in nuis-pixelmagic-filter-plan-v1 files own gray8 shape, payload bytes, ordered stages, scalar bindings, expected outputs, and dependency references. The nuis-pixelmagic-filter-plan-catalog-v1 package metadata declares two plans and one default; its AOT catalog parser requires complete static path coverage, unique paths and plan IDs, package ownership, a declared default, and a canonical hash over ordered path/id/source-hash entries. The open nuis-artifact-provider-metadata-v1 path carries ordered project-declared provider metadata through the build manifest, verified report, LinkPlan, runtime trace, and provider evidence without teaching the generic registry any PixelMagic plan IDs. Its nuis-artifact-provider-metadata-scope-v1 envelope optionally binds entries to domain and trace selectors, strips the envelope before package consumption, preserves global visibility for legacy entries, rejects malformed or duplicate selectors, and projects two plan requests independently for two traces in one artifact metadata table. PixelMagic validates every projected plan/hash pair, safely persists distinct plans with conflicting-path rejection, and remains the only layer that interprets plan IDs. Two official native routes run side by side: pixelmagic_pipeline_demo uses an exact shader/trace scope for invert/threshold, transfers [15,11,6,7] into threshold under GLM/time contracts, and compares [15,15,0,0]; the backward-compatible unscoped pixelmagic_threshold_provider_demo selects the non-default single-stage plan, executes one real Metal threshold request with zero dependency edges, and compares [0,0,15,15] with hash 0x4d00177f9dae564b. Both preserve deterministic fallback on unsupported hosts, while the original pipeline still owns final-image seal/replay coverage",
        next_step: "keep scoped plan selection regression-stable while additional pixel formats and backend families evolve through package-owned catalogs",
        blocker: "none for the current early-beta PixelMagic plan-selection scope; richer image ABIs, codecs, and backend-complete texture upload remain later package work",
        next_action: "advance to the next weakest standard-library coordinate while preserving independent scoped plan projection and legacy global metadata",
        validation_command: "cargo test -q -p nuis --test official_galaxy_hetero_smoke && cargo test -q -p nsdb provider_sample",
        expected_artifact: "artifact metadata can carry independently scoped PixelMagic plan requests for multiple shader traces, each package-authorized against the same hash-bound catalog, safely persisted together, and consumed without plan IDs entering the generic registry",
    },
    DevTensorCell {
        architecture: "standard-library",
        module: "witsage",
        function: "classical-ml-lane",
        status: "stable",
        progress: 100,
        bootstrap_critical: false,
        closure_role: "official-compute-package",
        evidence: "WitSage now emits seven ordered requests across five CoreML predictions and two Metal nodes. Linear feature-grid, affine, chained affine and two-input Add remain exact; the rank-three 16x64x64 f32 feature grid uses the tensor-contiguous v1 flat-span rule with matching 262144-byte stride and extent, while malformed declared request evidence fails closed before any provider output is written. The selected static provider bundle set contains both CoreML and Metal and is hash-verified through artifact-doctor. The cross-provider bias uses a package-owned non-zero tolerance profile and compares [11,17,23,29]. A second classical model family uses a real CoreML InnerProduct centroid scorer to map [1,2,3,4] into KMeans-equivalent scores [0,16] through an independent witsage.model-predict.f32 profile. A dependent generic Metal argmax consumes that transferable score carrier and emits exact u32 assignment 1; nuis-witsage-cluster-assignment-v1 keeps highest-centroid-score semantics inside the package while Nsdb remains unaware of KMeans. The CoreML -> Metal -> CoreML -> Metal ordering reuses CoreML worker sequence 4 and Metal sequence 1. Expected assets remain length/hash authenticated before comparison, both f32 profiles reject non-finite values, all seven comparisons pass, and all seven GLM output handles release at graph close",
        next_step: "keep the completed classical inference surface regression-stable while training and additional model families evolve as later package capabilities",
        blocker: "none for the current early-beta classical-inference scope; native training, larger clustering plans, and richer evaluation pipelines remain future package work",
        next_action: "advance to the next mainline coordinate while preserving package-owned assignment semantics and provider-neutral argmax execution",
        validation_command: "cargo test -q -p nuis --test official_galaxy_hetero_smoke && cargo test -q -p nsdb provider_sample",
        expected_artifact: "WitSage emits five CoreML and two Metal requests in an ordered collection, including a validated rank-three contiguous tensor, a verified two-bundle static selection set, fail-closed malformed request handling, real linear/fan-in/cross-provider paths, KMeans-equivalent centroid scores, exact u32 cluster assignment 1, package-owned comparison and assignment semantics, hash-bound expected outputs, indexed identities, graph/output hashes, and seven released GLM output handles",
    },
    DevTensorCell {
        architecture: "language-core",
        module: "nuisc",
        function: "type-control-flow-generics",
        status: "stable",
        progress: 100,
        bootstrap_critical: true,
        closure_role: "language-self-hosting-foundation",
        evidence: "control flow, generics, traits, pointers, floats, enums, error handling, inference, task_result_enum, generic trait-bound guarded-match, GLM buffer, generic_result_buffer_lambda helper with Err fallback, if-expression runtime coverage, nested partial expected-type Result.Ok propagation, nested if/match expected-type Result.Ok/Err propagation, project-source no-annotation higher-order try/await Result binding propagation, async_value YIR boundary lowering, runnable no-annotation try/await Result HOF Ok-path plus async/HOF Err-path binary coverage, true `?` Err continuation for inlined async Result helpers via select-survivor lowering, chained try/await Result HOF native binary coverage across normalize/decorate/pipeline helper boundaries, dynamic host argv input driving helper-side Result `?` continuations through Result-level early-return folding, std-style dynamic Result/Buffer/lambda/trait-bound/pointer bootstrap workload with native binary exit 59, import-boundary std-style workload with public helper module generic Result/HOF helpers, helper-side HOF synthetic functions, helper-module impl method emission for support-side trait-bound bump, inferred imported result_map and Result helper constructor generic arguments, three-module std-style package-boundary workload with helper-to-helper HOF templates, partial expected-type propagation, imported aliases, traits, buffers, pointers, and native binary exit 55, reusable std language contract modules `lib/language_core.ns` and `lib/language_ops.ns` consumed through `std=workspace` with std galaxy auto-injection and native binary exit 55, CLI-shaped std language report consumer combining those contracts with StdTextContracts and StdIoContracts while writing a real stdout report with native exit 0, std language report-file consumer combining the same language contracts with StdReportContracts while writing an argv-selected report file plus stdout with native exit 0, std language workflow consumer combining StdLanguageOps.build_report with a two-step host command workflow through StdCliContracts and native exit 0, std language build-pipeline consumer extending that route into a four-stage prepare/check/compile/package gate through StdCliContracts.build_pipeline_total with native exit 0, larger cli_build_pipeline consumer carrying the auto-injected language gate through NIR/YIR/LLVM and native exit 0 without deferred lowering, scheduler-backed zero/unary/binary i64 async thunks now share one normalized runtime packet and cover completed, timed-out, and cancelled native branches, including recursive binary thunk execution with native exit 14, recursive scalar-leaf struct tasks now encode complete nested layout trees, flatten declaration-ordered leaves into one self-describing scalar/blob slot allocation, and reconstruct nested field SSA through await, direct join, and TaskResult paths with mixed bool/integer/float/String native exit 37; String leaves copy UTF-8 bytes into shape-tokened owned blobs, re-intern on take, and are released by the aggregate drop hook; the native text boundary accepts valid multibyte UTF-8 and rejects overlong, surrogate, truncated, and out-of-range encodings; copy_bytes(ref Buffer) now creates an async-safe Bytes value, lowers to cpu.copy_buffer_owned, and interpreted YIR proves deep-copy independence after source mutation; aggregate finish is transactional and rolls back attached blobs on unset, duplicate, or invalid slots; NuisSchedulerOwnedBlobV1 deep-copies borrowed bytes, requires a GLM token, and crosses scheduler take/cancel paths through validated move/drop hooks; null owned payloads enter Failed(4), task_failed observes that terminal state, a compiled C harness proves the runtime transition, and native timeout/cancel paths prove the deferred aggregate helper never executes; straight-line Bytes locals now receive compiler-synthesized reverse-order cleanup after return-value evaluation while explicit drops and returned aggregate transfers remain exactly once; dynamic same-owner Bytes return branches lower through GLM-typed cpu.select_owned_bytes and a native LLVM ptr select with one-copy/one-drop accounting; return-producing if branches with nested extern-call comparisons now lower through compare_call_result into real guard_host_call_return LLVM blocks, and the persistent Nuis provider worker proves the unselected reply call is not executed; run-artifact JSON readiness assertions, explicit void-main return lowering, report-file main-return runtime coverage, and unconstrained Result.Ok/Err constructor diagnostics have staged CLI/IR/runtime coverage",
        next_step: "keep typed Node/Buffer owned-transfer metadata regression-stable while projected, nullable-transfer, returned, and task-carried address ownership remain explicit future capabilities",
        blocker: "none for the current early-beta language bootstrap scope; selected helper transfer now preserves address kind and nullability through YIR, CPU, and LLVM, while projected and task-carried pointer ownership remain intentionally closed rather than silently weakening GLM",
        next_action: "advance to the next bootstrap-critical coordinate while preserving typed owned-transfer metadata and exact-one address consumption",
        validation_command: "cargo test -q -p nuisc tests_owned_pointer_select && cargo test -q -p nuisc owned_pointer_transfer && cargo test -q -p nuisc --test effectful_if_compile && cargo test -q -p yir-core branch_effect && cargo test -q -p yir-domain-cpu branch_effect && cargo test -q -p yir-exec && cargo test -q -p yir-lower-llvm && cargo test -q -p nuis --test owned_pointer_select_smoke && cargo test -q -p nuis --test branch_effect_result_smoke && cargo check -q -p nuisc",
        expected_artifact: "language bootstrap regressions preserve generic Result/HOF inference, control-flow and async continuations, std Galaxy imports, native CLI/task execution, GLM ownership cleanup, and pointer selection; selected helper transfer encodes matching address_kind=node|buffer plus nullable=false metadata on every branch, CPU validates heap kind/nullability, LLVM preserves Node ptr and Buffer ptr+len ABI without deferred lowering, exact-one helper consumption holds, and owned_pointer_select_demo exits 94",
    },
    DevTensorCell {
        architecture: "host-compatibility",
        module: "cffi",
        function: "registered-pointer-string-object-boundary",
        status: "usable",
        progress: 93,
        bootstrap_critical: true,
        closure_role: "registered-host-memory-boundary",
        evidence: "official.cffi owns mod cffi source admission and registers six real hash-bound memory capabilities: five borrowed UTF-8 arguments plus one owned ref Buffer return. Exact signature and nuis-ffi-memory-v1 hashes bind ABI, slot, length, mutability, lifetime, and destructor authority before lowering and survive project metadata plus Nsld validation. The owned path has a dedicated Res-producing NIR/YIR operation whose self-verifying nuis-ffi-owned-buffer-v1 metadata recomputes producer, capability, and destructor hashes. LLVM calls the producer as ptr, traps on null or negative runtime-header length, carries ptr+len with the exact registered destructor, and cpu.free invokes that destructor once. A dedicated GLM Res/Own branch effect accepts two direct registered owners only when ABI, destructor symbol, and destructor signature hash match exactly, releases the unselected owner, and merges pointer plus runtime length. The new nuis-ffi-owned-buffer-function-transfer-v1 path lets one synchronous helper move a direct registered owner to its entry caller. YIR and the helper signature retain ABI/destructor/hash identity, the runtime ABI is exactly {ptr,i64}, CPU verification marks the producer moved and the result owned, and the caller must release it exactly once. Source helper chains may normalize to this single runtime boundary. Compiler and CLI regressions reject identity drift and missing cleanup, while a native artifact executes two allocations, helper returns, reads, and destructor calls with exit 0. Raw ptr<T>, pointer arithmetic, arbitrary ref T, retained borrows, runtime nested/recursive returns, task/async/loop-carried owners, and generalized external memory authority remain unavailable.",
        next_step: "extend the registered owner transfer protocol across one non-recursive nested synchronous helper boundary while preserving exact destructor identity and keeping raw pointer syntax closed",
        blocker: "direct release, one conditional transfer, and one direct helper-to-entry return are native and exact-once, but ownership cannot yet cross nested helpers, recursion, tasks, async work, or loops and no arbitrary ref T or retained-borrow authority exists",
        next_action: "add one nested helper-to-helper owner transfer with static identity propagation and prove that only the outermost caller can perform final cleanup",
        validation_command: "CARGO_INCREMENTAL=0 cargo test -q -p yir-verify && CARGO_INCREMENTAL=0 cargo test -q -p yir-domain-cpu branch_effect && CARGO_INCREMENTAL=0 cargo test -q -p yir-lower-llvm && CARGO_INCREMENTAL=0 cargo test -q -p nuisc pipeline_ffi_owned_buffer --lib && CARGO_INCREMENTAL=0 cargo test -q -p nuisc --test ffi_compile && CARGO_INCREMENTAL=0 cargo test -q -p nuis --test ffi_smoke ffi_libc_demos_build_and_run_as_native_artifacts",
        expected_artifact: "one non-recursive helper forwards a registered ref Buffer returned by another helper without exposing raw pointers; both helper boundaries preserve pointer, runtime length, ABI, destructor symbol, and hash identity while exactly one outer caller release remains possible",
    },
    DevTensorCell {
        architecture: "package-system",
        module: "galaxy",
        function: "source-import-and-lock-resolution",
        status: "usable",
        progress: 93,
        bootstrap_critical: true,
        closure_role: "bootstrap-package-resolution",
        evidence: "workspace Galaxy dependencies resolve through the stdlib index, transitive core/std dependencies and project-auto library modules enter project scope, manual-only modules require explicit imports, suggested imports can be applied deterministically, and manifests reject duplicate or undeclared imports. nuisc owns nuis-galaxy-resolution-lock-v1: the root nuis.galaxy.lock and generated nuis.project.galaxy.lock now share one canonical direct/transitive edge, package identity, manifest/source/library SHA-256, import-policy, and auto/explicit/hidden selection protocol. lock-deps writes it without physical paths, verify-lock compares it byte-for-byte with the current closure, and project-status reports its resolution digest plus direct/transitive package identities. Existing committed locks are verified before compile-cache restore, lowering, linking, or output creation, and the build snapshot is byte-identical to the accepted root lock. sync-deps re-reads every file through its frozen content identity into a staged .nuis/deps/galaxy tree, copies the canonical lock, transactionally replaces the old tree, removes unlocked residue, and preserves the prior tree on drift or staging failure. install-deps is the same lock-plus-sync route; local bundle publishing remains independent. Build-manifest verification still independently rejects generated lock payload or digest drift.",
        next_step: "make release admission require the root lock and let the compiler resolve from a content-addressed synchronized cache instead of treating the workspace as the primary package source",
        blocker: "an existing lock is fail-closed, but early-beta development builds still permit a missing lock and the resolver reads workspace bytes before comparing them with the committed snapshot; remote solving, registry discovery, and cache-owned resolution are not implemented",
        next_action: "add an explicit required-lock release policy, content-address materialized packages by resolution identity, and select that verified cache as the compiler resolver input without coupling nuisc to a specific registry backend",
        validation_command: "CARGO_INCREMENTAL=0 cargo test -q -p nuisc galaxy_resolution_lock --lib -j 1 && CARGO_INCREMENTAL=0 cargo test -q -p nuis galaxy -j 1 && cargo fmt --all -- --check",
        expected_artifact: "release builds require one committed nuis.galaxy.lock, synchronize its exact package closure into a content-addressed cache, and compile from that verified cache while local workspace, future remote registry, and offline sources remain interchangeable providers",
    },
    DevTensorCell {
        architecture: "native-binary-system",
        module: "nsb-nsld",
        function: "self-owned-binary-assembly",
        status: "stable",
        progress: 100,
        bootstrap_critical: true,
        closure_role: "self-owned-native-binary",
        evidence: "Nsld container, object/image dry-runs, final executable pipeline, self-contained NSB image emission, container-metadata-first image payload layout, container-payload relocation patching, launcher dry-run checks, host entrypoint handoff stub emission, host-runner image/container probe, Nsld pipeline self-owned image status, entrypoint materialization status, Nuis frontdoor consumption, run-artifact first-payload launch evidence, nsdb-facing payload execution trace records, persisted nsdb payload handoff metadata, nsdb inspect payload execution events, nsdb replay-plan checkpoint source/query-result fields, nsld final-output consumption of nsdb replay summary checkpoint counts, workflow closure_summary replay-ready/replay-blocked gate selection, nsdb-yir-replay-control-v1 adds deterministic frame and breakpoint stops, nsdb-yir-breakpoint-predicate-v1 matches typed phase and entry fields, nsdb-yir-replay-resume-cursor-v1 advertises the exact successor, nsdb-yir-replay-resume-input-v1 requires immediate stopped/next neighbors before suffix consumption, payload handoff events remain preferred while hetero runtime trace records provide a deterministic metadata/device-dispatch fallback, --save-cursor persists nsdb-yir-replay-cursor-record-v1 only for resumable stops, --resume-cursor validates record/transcript/source/manifest contracts before exact suffix consumption, nuis-debugger-cursor-handoff-v1 mirrors cursor path/readiness/status and a cursor-specific next command through final-output and closure summaries without importing Nsdb types, nuis debug-resume validates that handoff before structured-argv Nsdb dispatch and forwards exact/typed breakpoint plus optional cursor-save controls, nuis debug-lineage-repair rejects missing lineage before dispatch and drives the Nsdb-owned idempotent archive/rebuild path through the shared structured-argv executable boundary, PixelMagic emits real data/kernel/shader checkpoints and proves stop-save, resume-stop-replace, then replacement-cursor resume through the Nuis route, multi-data-unit stitching and validation use handle-table ownership plus window ancestry instead of global pipe order, cursor persistence writes and syncs a same-directory temporary file, validates it with the normal loader, atomically renames it, syncs the directory on Unix, preserves the old cursor on validation failure, self-contained NSB smoke rejects terminal cursors with zero consumption, repair journal v3 independently verifies canonical retained-event hash chains in Nsdb and Nuis, v4 anchors a full rotated window with a monotonic generation plus evicted-prefix hash so front truncation fails closed, Nuis preserves validated rotation evidence through final-output JSON/text and closure JSON, a canonical formatting-independent repair_window_hash binds protocol/path/rotation/window head-tail/lineage evidence across those persistent surfaces, v5 persists the Nsdb producer claim while both Nsdb and Nuis independently reject digest mismatch, provider sample materialization now upserts an idempotent provider-device-completion into the shared payload stream while Nsld final-output preserves it, provider-only early handoffs overlay matching hetero runtime traces so official multi-frame replay retains unrelated domain frames, backend-neutral provider_family/output_contract/output_evidence fields survive independent Nuis parsing, the ordered Metal/CoreML completion set carries per-record plus collection hashes through Nsld final-output and Nuis closure JSON, new handoffs persist the set hash as a producer claim, Nsdb and Nuis independently classify it as verified/legacy-unclaimed/mismatch, mismatch blocks replay, closure readiness, and subsequent upserts, explicit digest negotiation binds handoff protocol plus declared total/provider counts and ordered record hashes, new producer records and set claims use independently implemented SHA-256 set-v4 verification that also binds a whitelist-validated authority contract and producer id, explicit SHA set-v3, FNV set-v2, and no-contract set-v1 artifacts remain readable, unknown digest or authority contracts fail closed, the near-limit Nsdb handoff, independent Nuis parser, and Nsld final-output report are split into focused integrity/data modules without changing their protocols, and optional Ed25519 producer claims now bind signature-required canonical set-v5 evidence to an external signing key, fail closed on malformed/untrusted/tampered signatures, verify through independent Nsdb/Nuis implementations, remain metadata-only through Nsld and closure surfaces, an external trust-registry-v1 now validates nonzero generations, key-ID hashes, unique entries, active/revoked rotation, and malformed registry fail-closed behavior through independent Nsdb/Nuis parsers, independent trust-anchor-v1 implementations atomically persist the highest generation plus full registry hash, reject rollback and same-generation forks, and serialize cross-process updates, lock-v1 owner tokens and timestamps recover crashed writers after a bounded lease, the fail-closed file-v1 backend selector reserves an OS-protected adapter boundary, and marker-v1 independently records initialization so a surviving protected marker detects anchor deletion while existing anchors migrate safely",
        next_step: "keep protected-file-v1 regression-stable while platform Keychain, TPM, or service-backed anchors evolve as early-beta hardening adapters",
        blocker: "none for the current early-beta native-binary closure scope; privileged compromise of both protected roots remains outside a filesystem-only adapter and requires a future OS-protected backend",
        next_action: "advance to the next bootstrap-critical coordinate while preserving independent Nsdb/Nuis protected-root verification and Nsld metadata neutrality",
        validation_command: "cargo test -q -p nsdb trust_anchor && cargo test -q -p nsdb trust_registry && cargo test -q -p nuis trust_anchor && cargo test -q -p nuis trust_registry && cargo test -q -p nsld final_executable_output_command_persists_nsdb_handoff_record && cargo test -q -p nuis closure_summary && cargo test -q -p nuis dev_tensor",
        expected_artifact: "provider completion readiness requires SHA-256 integrity, a valid Ed25519 signature, a versioned external trust registry, and an atomic highest-generation/hash anchor with stale-lock recovery, marker-v1 deletion detection, plus independent protected-file-v1 enforcement of two explicit absolute 0700 roots and 0600 files while Nsld remains a neutral metadata consumer",
    },
    DevTensorCell {
        architecture: "native-binary-system",
        module: "nuis-runtime",
        function: "lifecycle-loader-bootstrap",
        status: "stable",
        progress: 100,
        bootstrap_critical: true,
        closure_role: "self-owned-runtime-entry",
        evidence: "nuis-runtime owns the fail-closed nuis-runtime-lifecycle-bootstrap-plan-v1 execution/transfer chain, RW-to-RX executable adapter, one-shot invocation permit, and fixed 64-byte nuis-runtime-lifecycle-entry-context-v1. Transfer records the context protocol and identity hash; independent reconstruction binds plan/execution identities plus clock-root, GLM-root, scheduler, and lifecycle handles. Permit authorization consumes that exact context with the RX mapping. Nsld emits calling-convention-specific AAPCS64, x86_64 SysV, and x86_64 Win64 entries and rejects unknown conventions; legacy context ABI v1 and dispatch-aware entry ABI v2 share the fixed context prefix while selecting explicit service-dispatch behavior. The explicit host-runner probe invokes the exact final-NSB entry once, while default validation remains non-invoking; return mismatch, lifecycle mismatch, context claim drift, missing services, code drift, foreign architecture, and ABI/binding mismatch all fail closed. The self-contained smoke proves nuis build -> nsld drive -> launcher resolution -> context-bound native invocation with context version 1 and return 0.",
        next_step: "keep the completed loader/bootstrap/context ABI regression-stable while runtime service dispatch evolves in its own coordinate",
        blocker: "none for the current early-beta lifecycle loader/bootstrap closure; native service invocation through the opaque context handles is tracked separately",
        next_action: "advance to lifecycle-context-dispatch while preserving transfer-declared context identity, one-shot permits, default non-invocation, and calling-convention-specific entry probes",
        validation_command: "CARGO_INCREMENTAL=0 cargo test -q -p nuis-runtime executable_memory && CARGO_INCREMENTAL=0 cargo test -q -p nuis-runtime native_entry_context && CARGO_INCREMENTAL=0 cargo test -q -p nuis-runtime lifecycle_bootstrap && CARGO_INCREMENTAL=0 cargo test -q -p nuis-runtime lifecycle_execution && CARGO_INCREMENTAL=0 cargo test -q -p nuis-host-runner && CARGO_INCREMENTAL=0 cargo test -q -p nsld native_entry && CARGO_INCREMENTAL=0 cargo test -q -p nuis --test self_contained_nsb_smoke && CARGO_INCREMENTAL=0 cargo test -q -p nuis dev_tensor",
        expected_artifact: "the final NSB carries a hash-verified native entry whose declared context ABI is reconstructed from one ready transfer, bound into one non-cloneable permit, passed as one immutable fixed-layout pointer, read by the architecture/calling-convention-specific thunk, and rejected before invocation on any identity, service, ABI, architecture, or lifecycle drift",
    },
    DevTensorCell {
        architecture: "native-binary-system",
        module: "nuis-runtime",
        function: "lifecycle-context-dispatch",
        status: "stable",
        progress: 100,
        bootstrap_critical: true,
        closure_role: "native-runtime-service-dispatch",
        evidence: "nuis-runtime owns the fixed-layout nuis-runtime-service-dispatch-table-v1 records and a 200-byte launch frame whose first 64 bytes preserve the stable lifecycle context ABI while table, request, and response offsets are locked at 64, 112, and 152. The provider-neutral nuis-runtime-dispatch-import-resolution-v1/binding-v1 contracts validate one required registered-runtime declaration, derive a stable import identity without a process address, include that identity in the dispatch execution hash, carry the materialized table identity and capability mask through CompiledEntryTransferResult, and bind the same identity into NativeEntryInvocationPermit plus executable preparation. Nsld marks generated entries with the dispatch-aware v2 ABI; the older v1 ABI is explicitly import-free rather than an implicit fallback. Missing, duplicate, forged, identity-drifted, table-drifted, and cross-context bindings invoke nothing. AAPCS64, x86_64 SysV, and x86_64 Win64 machine entries call the table handler. nuis-runtime-dispatch-receipt-v1 now binds the resolution protocol/status, execution and import identities, table identity, capability mask, slot, status code, acknowledgment, and a canonical FNV-1a receipt hash. Nuis captures it only from an invoked host runner, persists it without destroying provider completion records, and independently rejects malformed or tampered claims. Nsdb owns a separate parser and verifier, blocks invalid handoffs, preserves verified receipts across completion rewrites, and projects the result into replay summaries. Nsld consumes only that provider-neutral Nsdb summary, while both Nuis frontdoors share one live final-output projection from the verified handoff. The self-contained Nuis source -> build -> Nsld drive -> final NSB -> M2 AArch64 smoke executes the actual native entry and proves scheduler ACK slot 2/status 0 through handoff, final-output workflow JSON, and Nsdb replay.",
        next_step: "keep the completed dispatch receipt and independent replay verification regression-stable while additional runtime services and native host ABIs reuse the same provider-neutral contract",
        blocker: "none for the current early-beta lifecycle dispatch closure. Native Linux SysV and Windows Win64 execution remain explicit cross-host regression items rather than inferred local evidence.",
        next_action: "register the next milestone coordinate before resuming task selection; preserve the dispatch receipt contract and keep Linux SysV execution as an explicit deferred cross-host validation item",
        validation_command: "CARGO_INCREMENTAL=0 cargo test -q -p nuis-runtime && CARGO_INCREMENTAL=0 cargo test -q -p nuis-host-runner && CARGO_INCREMENTAL=0 cargo test -q -p nsdb runtime_dispatch_receipt && CARGO_INCREMENTAL=0 cargo test -q -p nsld && CARGO_INCREMENTAL=0 cargo test -q -p nuis --test self_contained_nsb_smoke && CARGO_INCREMENTAL=0 cargo test -q -p nuis dev_tensor",
        expected_artifact: "one independently verifiable nuis-runtime-dispatch-receipt-v1 survives host execution, final-output projection, Nsdb handoff, Nsdb replay, and Nsld consumption with execution-bound import/table/capability/slot/status/ACK identities intact; missing, forged, stale, cross-context, or replay-drifted receipts authorize no lifecycle service call",
    },
    DevTensorCell {
        architecture: "developer-system",
        module: "dev-tensor",
        function: "architecture-module-function-progress-model",
        status: "stable",
        progress: 100,
        bootstrap_critical: true,
        closure_role: "bootstrap-progress-model",
        evidence: "tensor frontdoor maps progress by architecture, module, and function, summarizes through nuis status, runs drift checks over key docs/tests/frontdoor fields including the provider-neutral Nsld executable-finalizer registry and its invoke-plan projection, locks the current release entry, repository README, and mainline router against version drift, exposes weakest-bootstrap guidance plus a protocolized nuis-dev-tensor-task-card-v1 surface, prioritizes incomplete bootstrap-critical cells and then falls through to the globally weakest incomplete cell once every bootstrap-critical coordinate is stable at 100/100, selects task and self-maintenance handoff cells through deterministic status-rank/progress/coordinate ordering instead of registration order, mirrors task-card protocol/source/status/ready/coordinate/action/command/expected artifact through nuis status, reports stdlib manifest-backed coordinate coverage, validates milestone-owned coordinate coverage against the Rust snapshot, derives expected coordinates from the milestone manifest with an explicit fallback, surfaces generated coordinate cache metadata without writing cache files, enforces the repository UTF-8/LF source-text contract through the mainline gate, validates the complete root/architecture/module/function projection through nuis-dev-tensor-hierarchy-v1, and binds task plus handoff coordinates back to recursively reachable function leaves through nuis-dev-tensor-task-card-lineage-v1 with complete ancestry, common-ancestor, transition-depth, mode, and readiness invariants",
        next_step: "keep task-card lineage, hierarchy, coverage, and drift clean while beta foundation coordinates advance through the normal bootstrap-first selector",
        blocker: "none for the current repository-local recursive handoff scope; federated cross-project hierarchy identity and distributed status sources remain future protocol extensions",
        next_action: "preserve deterministic selection and milestone coverage while the completed Galaxy root-lock tranche hands work back to linker-toolchain/nsld/os-native-executable-finalization",
        validation_command: "python3 scripts/check-text-encoding.py && cargo test -q -p nuis dev_tensor",
        expected_artifact: "dev-tensor reports milestone-derived coordinate cache metadata, clean hierarchy and nuis-dev-tensor-task-card-lineage-v1 validation, bootstrap-first then global-incomplete task selection while work remains, deterministic handoff independent of registration order, an honest complete terminal state only when every registered slice closes, and a UTF-8/LF mainline gate",
    },
    DevTensorCell {
        architecture: "developer-system",
        module: "dev-tensor",
        function: "self-hosting-phase-roadmap",
        status: "stable",
        progress: 100,
        bootstrap_critical: true,
        closure_role: "self-hosting-phase-governance",
        evidence: "nuis-self-hosting-phase-roadmap-v1 fixes three honest phase boundaries: beta-0.0.1 through beta-0.9.* is foundation-readiness for closure work, bug fixing, performance, and stability; beta-0.10.* starts the formal stage0-to-stage1 self-hosting migration; gamma-0.5.* is the target for stage2-equivalent self-hosting completion after an approximately fifteen-minor engineering horizon",
        next_step: "keep foundation-readiness as the mainline through beta-0.9.* without prematurely claiming compiler self-hosting, then activate staged compiler migration at beta-0.10.*",
        blocker: "none for roadmap governance; self-hosting implementation intentionally remains future work until the beta-0.10.* phase boundary",
        next_action: "preserve the foundation-first boundary in current task selection and require future beta/gamma milestone manifests to advance the same protocol rather than inventing a parallel self-hosting schedule",
        validation_command: "cargo test -q -p nuis dev_tensor",
        expected_artifact: "dev-tensor JSON/text and milestone coverage expose developer-system/dev-tensor/self-hosting-phase-roadmap with the foundation-readiness, beta-0.10.* migration-start, and gamma-0.5.* completion-target boundaries while stable/100 describes roadmap agreement rather than completed self-hosting",
    },
];

// Milestone IDs retain the phase that established each coordinate.
pub(crate) const DEV_TENSOR_EXPECTED_COORDINATES: &[DevTensorExpectedCoordinate] = &[
    DevTensorExpectedCoordinate {
        architecture: "compiler-frontdoor",
        module: "nuis-cli",
        function: "workflow-and-project-orientation",
        milestone: "alpha-frontdoor",
        required: true,
    },
    DevTensorExpectedCoordinate {
        architecture: "compiler-frontdoor",
        module: "nuis-cli",
        function: "artifact-runtime-closure",
        milestone: "alpha-frontdoor",
        required: true,
    },
    DevTensorExpectedCoordinate {
        architecture: "linker-toolchain",
        module: "nsld",
        function: "artifact-chain-drive",
        milestone: "alpha-linker",
        required: true,
    },
    DevTensorExpectedCoordinate {
        architecture: "linker-toolchain",
        module: "nsld",
        function: "final-output-boundary",
        milestone: "alpha-linker",
        required: true,
    },
    DevTensorExpectedCoordinate {
        architecture: "linker-toolchain",
        module: "nsld",
        function: "os-native-executable-finalization",
        milestone: "beta-0.1-foundation-hardening",
        required: true,
    },
    DevTensorExpectedCoordinate {
        architecture: "heterogeneous-runtime",
        module: "nustar",
        function: "registered-domain-contracts",
        milestone: "alpha-heterogeneous",
        required: true,
    },
    DevTensorExpectedCoordinate {
        architecture: "heterogeneous-runtime",
        module: "linux-cuda",
        function: "cuda-provider-bringup",
        milestone: "alpha-linux-cuda",
        required: false,
    },
    DevTensorExpectedCoordinate {
        architecture: "heterogeneous-runtime",
        module: "linux-vulkan",
        function: "vulkan-provider-bringup",
        milestone: "alpha-linux-vulkan",
        required: false,
    },
    DevTensorExpectedCoordinate {
        architecture: "heterogeneous-runtime",
        module: "data",
        function: "provider-neutral-data-fabric",
        milestone: "beta-0.1-data-provider-direction",
        required: false,
    },
    DevTensorExpectedCoordinate {
        architecture: "standard-library",
        module: "std",
        function: "host-io-filesystem-text",
        milestone: "alpha-stdlib",
        required: true,
    },
    DevTensorExpectedCoordinate {
        architecture: "standard-library",
        module: "std",
        function: "concurrency-task-thread-lock",
        milestone: "beta-0.1-foundation-hardening",
        required: true,
    },
    DevTensorExpectedCoordinate {
        architecture: "standard-library",
        module: "pixelmagic",
        function: "image-processing-lane",
        milestone: "alpha-official-galaxy",
        required: false,
    },
    DevTensorExpectedCoordinate {
        architecture: "standard-library",
        module: "witsage",
        function: "classical-ml-lane",
        milestone: "alpha-official-galaxy",
        required: false,
    },
    DevTensorExpectedCoordinate {
        architecture: "language-core",
        module: "nuisc",
        function: "type-control-flow-generics",
        milestone: "alpha-language-core",
        required: true,
    },
    DevTensorExpectedCoordinate {
        architecture: "host-compatibility",
        module: "cffi",
        function: "registered-pointer-string-object-boundary",
        milestone: "beta-0.1-foundation-hardening",
        required: true,
    },
    DevTensorExpectedCoordinate {
        architecture: "package-system",
        module: "galaxy",
        function: "source-import-and-lock-resolution",
        milestone: "beta-0.1-foundation-hardening",
        required: true,
    },
    DevTensorExpectedCoordinate {
        architecture: "native-binary-system",
        module: "nsb-nsld",
        function: "self-owned-binary-assembly",
        milestone: "alpha-native-binary",
        required: true,
    },
    DevTensorExpectedCoordinate {
        architecture: "native-binary-system",
        module: "nuis-runtime",
        function: "lifecycle-loader-bootstrap",
        milestone: "beta-runtime-loader",
        required: true,
    },
    DevTensorExpectedCoordinate {
        architecture: "native-binary-system",
        module: "nuis-runtime",
        function: "lifecycle-context-dispatch",
        milestone: "beta-runtime-loader",
        required: true,
    },
    DevTensorExpectedCoordinate {
        architecture: "developer-system",
        module: "dev-tensor",
        function: "architecture-module-function-progress-model",
        milestone: "alpha-governance",
        required: true,
    },
    DevTensorExpectedCoordinate {
        architecture: "developer-system",
        module: "dev-tensor",
        function: "self-hosting-phase-roadmap",
        milestone: "self-hosting-roadmap",
        required: true,
    },
];
